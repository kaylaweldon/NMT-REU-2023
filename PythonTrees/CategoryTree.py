class CategoryTree:

    # initialize a tree with pointer to root node and a list
    # need to add code to actually create the tree
    def __init__(self, list):
        self.treeList = list

    # add code to normalize a tree, i.e. sort nodes on each level
    # from least children to most children
    def normalize(self):

    # print out the representation of the tree
    def printTree(self):
    
    def printListOfTree(self):
        